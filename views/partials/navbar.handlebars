<!-- Nav Bar Section -->
<nav class="navbar navbar-expand-md navbar-light bg-light mb-3">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler"
            aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="row">
            <div class="d-inline col-sm-3 text-start">
                <a class="navbar-brand text-start" href="/">Reserve-A-Room</a>
            </div>
            <div class="d-inline col-sm-9 text-end collapse navbar-collapse" id="navbarToggler">
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/">Home</a>
                    </li>
                    {{#unless logged_in}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    {{/unless}}
                    {{#if logged_in}}
                    <li class="nav-item">
                        <a class="nav-link" href="/profile" tabindex="0">My Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout" tabindex="-1">Logout</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </div>
</nav>